In `ClientCard.tsx`, read the trainer plan via `useTrainerPlan()` and, when plan is `basic`, hide the program and package badges (Strength Program, 1 Active Package, 1 Package). Keep the goal badges (Lose 5kg, Run 10K) since basic plan still has goals/check-ins.